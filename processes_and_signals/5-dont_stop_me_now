#!/usr/bin/env bash
# Stop the 4-to_infinity_and_beyond process using kill

# Find the PID and kill it using kill command
pid=$(pgrep -f "4-to_infinity_and_beyond")
if [ -n "$pid" ]; then
    kill "$pid"
fi
